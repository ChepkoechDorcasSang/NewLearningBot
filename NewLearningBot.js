function generateNumber(inputNumber){
    for(let i=1; i<=inputNumber; i++){
        let pattern=;
        for(let j=1;j<=i;j++){
            pattern +=i;
        }
        console.log(pattern);
    }
}
